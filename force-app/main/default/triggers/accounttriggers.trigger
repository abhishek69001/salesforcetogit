trigger accounttriggers on Account (after insert, after update) {
    //triggerspractice.insertcontact(trigger.new);
      //triggerspractice.duplicateprevent(trigger.new);
      //Triggerspractice.preventdelete(trigger.old);
      //Triggerspractice.phonevalidation(trigger.new);
       // Triggerspractice.updatecontact(trigger.new);
       // Triggerspractice.updatedescription(trigger.new);
            //Triggerspractice.insertrelatedcon(trigger.new);
            //Triggerspractice.updatecon(trigger.new);
        //Triggerspractice.updateindustry(trigger.new);
      // Triggerspractice.oppcreation(trigger.new);
    	//preventdeletionclass.adddeleterror(trigger.old);
    	
   // Triggerspractice.distribute(trigger.new);
    //Triggerspractice.insertmultiplecon(trigger.new);
   // Triggerspractice.createopp(trigger.new);
	//Triggerspractice.preventdeletion(trigger.old);
	
	
      
}