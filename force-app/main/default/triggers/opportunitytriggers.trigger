trigger opportunitytriggers on Opportunity (after update) {
   // triggerspractice.accratinghot(trigger.new);
        //triggerspractice.accrating(trigger.new);
        //triggerspractice.deleterel(trigger.old);
    	triggerspractice.oppo(trigger.new);

}