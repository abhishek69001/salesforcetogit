trigger contacttriggers on Contact (after insert) {
    //Triggerspractice.updateaccount(trigger.new);
    //Triggerspractice.updateaccount(trigger.new);
      //Triggerspractice.updatecontact(trigger.new);
      //Triggerspractice.insertaccount(trigger.new);
    //Triggerspractice.autoemailphone(trigger.new);
       // Triggerspractice.errorcreation(trigger.new);
       /* List<Contact> newContacts = new List<Contact>();
    for (Contact con : Trigger.new) {
        newContacts.add(con);
    }
    Triggerspractice.insertacc(newContacts);*/
      //    Triggerspractice.insertacc(trigger.new);
        
      /*    List<Contact> newContacts = new List<Contact>();
    for (Contact con : Trigger.new) {
        newContacts.add(con);
    }
        Triggerspractice.split(newContacts);*/
   // Triggerspractice.split(trigger.new);
   //Triggerspractice.makeprimary(trigger.new);
   //Triggerspractice.create(trigger.new);
}